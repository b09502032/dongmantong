# dongmantong

```
usage: main.py [-h] --train TRAIN [--validation VALIDATION]
               [--pretrained-model-name PRETRAINED_MODEL_NAME] [--log LOG] [--num-epochs NUM_EPOCHS]
               [--batch-size BATCH_SIZE] [--lr LR] [--seed SEED] [--fp16] [--device DEVICE]

optional arguments:
  -h, --help            show this help message and exit
  --train TRAIN         path to train problem set (default: None)
  --validation VALIDATION
                        path to validation problem set (default: None)
  --pretrained-model-name PRETRAINED_MODEL_NAME
                        (default: hfl/chinese-macbert-large)
  --log LOG             log directory (default: None)
  --num-epochs NUM_EPOCHS
                        (default: 100)
  --batch-size BATCH_SIZE
                        (default: 16)
  --lr LR               (default: 5e-06)
  --seed SEED           (default: 713574848)
  --fp16
  --device DEVICE       (default: None)
```

